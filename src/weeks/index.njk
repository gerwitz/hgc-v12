---
title: Weeks
layout: page
tags:
pagination:
  data: collections.weeks
  size: 52
  reverse: true
permalink: /weeks/{% if pagination.pageNumber > 0 %}page-{{ pagination.pageNumber }}/{% endif %}index.html
eleventyExcludeFromCollections: true
---

This is an experimental journal of sorts, so I can search for entries and experiment with a more holistic archive.

{% if pagination.pageNumber > 0 %}
<div class="pagination">
  <a href="{{ pagination.previousPageHref }}">Newer</a>
</div>
{% endif %}

<ol>
{% for week in pagination.items %}
  <li>
    {{ week.fileSlug | moonforweek }} <a href="{{ week.url }}">Week {{ week.fileSlug }}</a> ({{ week.fileSlug | weekstart | date('nice') }})
  </li>
{% endfor %}
</ol>

{% if pagination.nextPageHref %}
<div class="pagination">
  <a href="{{ pagination.nextPageHref }}">Older</a>
</div>
{% endif %}
