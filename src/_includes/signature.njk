<div class="signature">
  <p>
    I posted this
    {% if external -%}
    on <a href="{{external}}">{{ external | hostname }}</a>
    {%- endif %}
    in {{ page.date | date('MMMM YYYY') }} during week {{ page.date | weeklink }}.
  </p><p>
    For more, you should <a href="https://social.gerwitz.com/main/ostatus?nickname=hans" rel="me noreferrer nofollow">follow me on the fediverse</a>.
  </p>
</div>
