---
title: Events
layout: index
override:tags: []
eleventyExcludeFromCollections: true
---
{# types: [life, home, school, geek, work, car] #}

<p>
  Notable events in my life:
</p>

<section class="timeline reverse">
{% set lastYear = 0 %}
<ol>
{% for event in collections.events | reverse %}
  {% set year = event.date | date('YYYY') %}
  <li class="timeline-entry shown" data-year="{{ year }}" data-type="event">
    <time datetime="{{ event.date | date('iso8601') }}" data-year="{{ year }}" class="year{{ ' first' if (year != lastYear) }}">
      {{ year }}
    </time>
    <div>
      <a href="{{ event.url }}">{{ event.data.title }}</a>
      <div class="meta">
        <time datetime="{{ event.date | date('iso8601') }}">{{ event.date | date('nice') }}</time>
      </div>
    </div>
  </li>
  {% set lastYear = year %}
{% endfor %}
</ol>

</section>
