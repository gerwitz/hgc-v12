---
layout: base
bodyClass: week
breadcrumbs:
    - weeks
---

<article>

    <div style="position: relative">
        <h1>{{ renderData.title }}</h1>
        <span class="marginnote">
            [{{ renderData.weeknum }}] Including {{ renderData.weeknum | weekrange }}
        </span>
    </div>

    {{ content | safe }}

    {% if steps %}
        🚶🏻‍{{ steps }} steps
    {% endif %}

</article>
